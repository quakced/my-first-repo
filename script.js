let csProjectButton = document.querySelector(".csProjectButton")
let csProjectPage = document.querySelector(".csProjectPage")
let contactButton = document.querySelector(".contactButton")
let contactPage = document.querySelector(".contactPage")
let infoButton = document.querySelector(".infoButton")
let homeButton = document.querySelector(".homeButton")
let homePage = document.querySelector(".homePage")
let aboutMeButton = document.querySelector(".aboutMeButton")
let aboutMePage  = document.querySelector(".aboutMePage")
csProjectButton.addEventListener('click', function(){
csProjectPage.style.display = "block"
});
contactButton.addEventListener('click', function(){

});
aboutMeButton.addEventListener('click',function(){

});
homeButton.addEventListener('click',function(){

});
// Close the dropdown menu if the user clicks outside of it
/*window.onclick = function(event) {
    if (!event.target.matches('.dropbtn')) {
      var dropdowns = document.getElementsByClassName("dropdown-content");
      var i;
      for (i = 0; i < dropdowns.length; i++) {
        var openDropdown = dropdowns[i];
        if (openDropdown.classList.contains('show')) {
          openDropdown.classList.remove('show');
        }
      }
    }
  }
    */

  